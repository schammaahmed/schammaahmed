__pycache__/
*.pyc

# Regenerated by the workflow on every run before the SVGs are rendered, so
# there is no reason to keep a permanent machine-readable log of daily activity
# in git history. The public contribution graph is the source of truth.
data/
